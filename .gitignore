node_modules/
dist/

.env
.env.local

test-results/
playwright-report/
blob-report/
allure-results/
allure-report/

# Output of `npm run generate:test` — review before committing, don't auto-track
src/pages/generated/
tests/ui/generated/

*.log
.DS_Store

# Claude Code / agent-skill tooling used to build this repo — not part of the deliverable
.claude/
.agents/
skills-lock.json
